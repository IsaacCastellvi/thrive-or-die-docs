#### Signature

```cs
static void Load()
```

#### Description

Loads the player settings.

If there is no custom settings it loads the default setting and then saves them to persistent memory
If the settings file exists, it instead loads the file settings into memory
