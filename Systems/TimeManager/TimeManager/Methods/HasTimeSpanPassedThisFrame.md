#### Signature

```cs
static bool HasTimeSpanPassedThisFrame(TimeSpan timeSpan)
```

#### Parameters

- **timeSpan** The time span to check

#### Description

Checks and returns if the passed `TimeSpan` (as a time stamp) has past in between the last and current frame
